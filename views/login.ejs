<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no, viewport-fit=cover"
    />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
    <link rel="stylesheet" href=" https://cdnjs.cloudflare.com/ajax/libs/bootstrap-social/4.12.0/bootstrap-social.min.css">
    <link rel="stylesheet" href="/stylesheets/bootstrap-social.css" />
    <link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">  
  </head>
  <body>
      <div class="form">
        <div class="row">
          <h2><%= title %></h2>
        <form class="col-6" id="login_div" action="/login" method="POST">
          <input type="text" name="username" placeholder="Username" required />
          <input
            type="password"
            name="password"
            placeholder="Password"
            required
          />
          <% if (hasMessages) { %> <% messages.forEach(function(message){ %>
          <small><%= message %></small>
          <% }); %> <% } %>
            <div class="left">
              <button type="submit" id="login">Login</button>
              <div class="row" >
                <a class="btn-sub col-6" href="/signup">Register</a>
                <a class="btn-sub col-6" style="display: flex; justify-content: right;" href="/sendmail_forget">Forget Password</a>
              </div>
            </div>
        </form>
        <div class="right col-6">
          <a class="btn btn-block btn-social btn-google" href="/login/google">
            <span class = "fa fa-google"> </span> Login with Google
          </a>
          <a class="btn btn-block btn-social btn-facebook" href="/login/facebook">
            <span class = "fa fa-facebook"> </span> Login with Facebook
          </a>
          <a class="btn btn-block btn-social btn-github" href="/login/github">
            <span class = "fa fa-github"> </span> Login with Github
          </a>
        </div>
      </div>
  </body>
</html>
