<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no, viewport-fit=cover"
    />
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <div class="form">
      <form class="login-form" id="login_div" action="/sendmail_forget" method="POST">
        <h2><%= title %></h2>
        <input type="email" placeholder="Email address" name="email" id="email_forgot" />
        <% if(err){ %> 
        <small ><%= err %> </small>
        <%} %> 
        <button type="submit" id="login">Submit</button>
      </form>
    </div>
  </body>
</html>
