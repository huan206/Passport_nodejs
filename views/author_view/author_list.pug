extends ../layout/layout

block content
  h1= title

  ul
    each author in author_list
      li
        a(href=author.url) #{author.name}
        |  ( #{author.date_of_birth_show} - #{author.date_of_death_show} )

    else
      li There are no authors.